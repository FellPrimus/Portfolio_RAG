apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rag-service-data
  namespace: portfolio
  labels:
    app: rag-service
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
